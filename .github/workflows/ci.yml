name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    uses: ./.github/workflows/_validation.yml
    with:
      base-ref: ${{ github.base_ref }}
